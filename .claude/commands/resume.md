# /resume — reboot session context

Same reboot procedure as `new_session_prompt.md` (that file is the copy-paste fallback for environments without slash commands). Run these steps:

1. Read `CLAUDE.md`, then `handoff.md`, then the active feature file named in handoff.md's `🔗 Pointer` section — in that exact order.
2. Summarize your understanding back to Cam in exactly 3 bullets: (a) current project state, (b) the active feature, (c) what "Next Up" proposes.
3. Do NOT begin any work. Confirm the "Next Up" items with Cam and wait for a clear go-ahead before touching code or docs.
