/**
 * Dungeon Cascades engine version marker.
 *
 * PURE ENGINE: this module — and everything under `src/engine` — must never
 * import React or React Native, and must stay deterministic (seeded RNG only).
 * See CLAUDE.md.
 */
export const ENGINE_VERSION = '0.1.0';
